const express = require('express')
const router = express.Router()

const serialPortController = require('../controllers/serialport_controller')

// router.get('/', serialPortController.callDevice)

module.exports = router
